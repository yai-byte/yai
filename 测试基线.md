# 测试基线

建立时间：2026-07-22 16:34:55 CST

本文记录质量整改开始前的当前测试状态，用作后续拆分复杂函数和重构命令流程时的回归对照。基线只记录构建与现有 smoke test，不代表功能完整性已经超出当前测试覆盖。

## 1. 环境与范围

- 工作目录：`/home/fsx/yai`
- 构建目标：`yai`
- 测试范围：`README.md` 与 `修改措施.md` 中列出的全部 smoke test
- Git 状态：当前目录不是 git 仓库，无法记录 `git status`

## 2. 执行命令

```bash
make
```

```bash
tests/arch_smoke.sh
tests/json_smoke.sh
tests/download_smoke.sh
tests/stage1_smoke.sh
tests/stage2_smoke.sh
tests/stage3_smoke.sh
tests/stage4_smoke.sh
tests/stage5_smoke.sh
tests/repo_smoke.sh
tests/appimage_feed_smoke.sh
tests/mirror_policy_smoke.sh
tests/metadata_json_smoke.sh
```

额外检查：

```bash
./yai --help
```

## 3. 当前结果

| 项目 | 结果 | 关键输出 |
|---|---:|---|
| `make` | 通过 | `make: Nothing to be done for 'all'.` |
| `tests/arch_smoke.sh` | 通过 | `arch smoke test passed` |
| `tests/json_smoke.sh` | 通过 | `json smoke test passed` |
| `tests/download_smoke.sh` | 通过 | `download smoke test passed` |
| `tests/stage1_smoke.sh` | 通过 | `stage1 smoke test passed` |
| `tests/stage2_smoke.sh` | 通过 | `stage2 smoke test passed` |
| `tests/stage3_smoke.sh` | 通过 | `stage3 smoke test passed` |
| `tests/stage4_smoke.sh` | 通过 | `stage4 smoke test passed` |
| `tests/stage5_smoke.sh` | 通过 | `stage5 smoke test passed` |
| `tests/repo_smoke.sh` | 通过 | `repo smoke test passed` |
| `tests/appimage_feed_smoke.sh` | 通过 | `appimage feed smoke test passed` |
| `tests/mirror_policy_smoke.sh` | 通过 | `mirror policy smoke test passed` |
| `tests/metadata_json_smoke.sh` | 通过 | `metadata json smoke test passed` |
| `./yai --help` | 通过 | 输出中文帮助，包含 search、info、repo、mirror、download、install、update、rollback、repair、doctor、remove、list、help |

结论：当前构建和全部 smoke test 均通过。

## 4. 后续重构的回归要求

后续每完成一个整改阶段，应至少重复运行对应测试：

- `src/main.cpp` 命令派发拆分：
  - `make`
  - `./yai --help`
  - `tests/stage1_smoke.sh`
  - `tests/repo_smoke.sh`
- `src/appimage.cpp` desktop / wrapper / runtime 相关拆分：
  - `make`
  - `tests/stage1_smoke.sh`
  - `tests/stage2_smoke.sh`
  - `tests/metadata_json_smoke.sh`
- `src/resolver.cpp` 来源解析和 website 搜索拆分：
  - `make`
  - `tests/stage3_smoke.sh`
  - `tests/stage4_smoke.sh`
  - `tests/appimage_feed_smoke.sh`
- `src/commands.cpp` update / rollback / repo / mirror 相关拆分：
  - `make`
  - `tests/stage5_smoke.sh`
  - `tests/repo_smoke.sh`
  - `tests/mirror_policy_smoke.sh`
- `src/cli_download.cpp` 下载选项解析拆分：
  - `make`
  - `tests/download_smoke.sh`
  - `tests/stage1_smoke.sh`
- `src/json.cpp` JSON 辅助解析拆分：
  - `make`
  - `tests/json_smoke.sh`
  - `tests/stage3_smoke.sh`
  - `tests/stage4_smoke.sh`
- `src/core.cpp` 进程输出和下载进度拆分：
  - `make`
  - `tests/download_smoke.sh`
  - `tests/stage3_smoke.sh`
  - `tests/appimage_feed_smoke.sh`
- `src/arch.cpp` 架构评分拆分：
  - `make`
  - `tests/arch_smoke.sh`
- `src/repo.cpp` feed 归一化拆分：
  - `make`
  - `tests/repo_smoke.sh`
  - `tests/appimage_feed_smoke.sh`

全部整改完成后，重新运行第 2 节中的完整命令列表。

