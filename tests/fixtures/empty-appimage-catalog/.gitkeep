Places a directory under git so the AppImageHub catalog base can be pointed at a
local path. Fetching <dir>/<Name>/ fails with a file error, which is the same
code path a missing network produces, but without leaving the machine.
